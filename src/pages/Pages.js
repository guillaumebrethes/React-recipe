//  === Import  ===;

//  === Import : local  ===;
import Home from './Home';
//  === Component ===;
const Pages = () => {
  console.log('Composant Pages => ', Pages);

  return (
    <div>
      <Home />
    </div>
  );
};

export default Pages;
